on:
  push:
    branches: [ test ] 
name: 🚀 PomodoroPal - Deployment

jobs:

  web-deploy:

    name: 🎉 PomodoroPal - Deployment

    runs-on: ubuntu-latest

    steps:

    - name: 🚚 Get latest code

      uses: actions/checkout@v2
    
    - name: Sync files

      uses: SamKirkland/FTP-Deploy-Action@4.1.0

      with:

        server: 

        username: 

        password: 

        protocol: ftp
        
        server-dir: /public_html/